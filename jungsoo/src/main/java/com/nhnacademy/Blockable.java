package com.nhnacademy;

/**
 * Blockable
 * <p>
 * - 이 인터페이스를 구현하는 클래스는 '차단(블록) 가능한' 성격을 갖는다고 볼 수 있습니다.
 * - 예를 들어, Obstacle(장애물) 등이 Blockable을 구현해,
 *   공(Ball)과의 충돌이나 차단 로직을 처리할 수 있습니다.
 * - 구체적인 차단 방식(충돌 후 무슨 일이 일어나는지)은
 *   구현체에서 결정합니다.
 */
public interface Blockable {
    // 여기는 실제 메서드 없이, '마커' 성격의 인터페이스로 보일 수 있습니다.
    // 필요한 경우, Blockable 특유의 메서드를 선언하여 구현을 강제할 수도 있습니다.
}